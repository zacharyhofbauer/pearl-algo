# 2026-03-26 Session Journal

## Changes Deployed
1. **5m MTF Regime Detection** — regime computed on 5m bars, wired through service_loop → strategy.analyze → generate_signals. Falls back to 1m if <30 5m bars.
2. **TOD Confidence Scaling** — overnight 0.60x, evening 0.70x, RTH 1.0x. Config: `pearl_bot_auto.tod_scaling`
3. **Trailing Stop Sync Recovery** — miss counter (3=re-place, 6=deregister), position close cleanup, new `place_stop_order()` on adapter
4. **IBKR AutoRestart=yes** — no more daily 2FA at nightly reset
5. **IBKR datetime fix** — tz-aware comparison in ibkr_provider.py
6. **Dashboard rebuild** — static assets copied after other session broke build

## Root Cause Analysis (Mar 25 -$414)
- 92% of losses from midnight-2am ET chop (29 trades, near-0% WR)
- Direction gating stuck on "trending_up" from 1m while price crashed 140pts
- Trailing stop lost its stop order, auto-disarm locked out all trading
- Circuit breaker cascaded 6+ times overnight

## Files Modified
- `src/pearlalgo/trading_bots/pearl_bot_auto.py` (MTF regime, TOD scaling)
- `src/pearlalgo/market_agent/service.py` (strategy adapter, stop order recovery, cleanup)
- `src/pearlalgo/market_agent/service_loop.py` (passes df_5m)
- `src/pearlalgo/execution/tradovate/adapter.py` (place_stop_order)
- `config/accounts/tradovate_paper.yaml` (tod_scaling config)
- `config/base.yaml` (tod_scaling disabled default)
- `src/pearlalgo/data_providers/ibkr/ibkr_provider.py` (datetime fix)
- `/home/pearlalgo/ibkr/ibc/config-auto.ini` (AutoRestart=yes)

## Tests
- 377+ passed, 0 new failures
- 3 pre-existing failures (test_quiet_reason, test_service_high_risk_methods, test_signal_pipeline_integration)

---

## Session 2 — Deep Cleanup & Dead Code Purge

### Changes
- Removed 33K lines of dead code (ML/learning, telegram bot, server_core, backups, root junk)
- Archived 23 one-off scripts to `scripts/_archived/`
- Inlined health routes into `server.py` (eliminated circular import)
- Applied stashed webapp tweaks (indicator toggles, CSS)
- Dropped 2 obsolete git stashes

### Backend Structure (clean, feature-based)
```
src/pearlalgo/
  api/             — FastAPI server
  analytics/       — offline analysis
  config/          — config loading
  data_providers/  — IBKR market data
  execution/       — Tradovate order execution
  market_agent/    — trading agent runtime
  notifications/   — telegram alerts (send-only)
  storage/         — async SQLite queue
  trading_bots/    — signal generation
  utils/           — shared utilities
```

### Status
- All services restarted on clean master
- Tests passing (66 passed)
- Pushed to GitHub
- OpenClaw tunnel active, memory bridged to Pearl Mac
