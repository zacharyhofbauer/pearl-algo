# Tests allowed to import from the legacy `pearlalgo.trading_bots` package.
#
# The long-term direction (Issue 1-A in
# ~/.claude/plans/this-session-work-cosmic-horizon.md) is to extract
# `trading_bots/signal_generator.py` into `strategies/composite_intraday/*`
# modules. Tests that exercise the legacy surface are the hardest blocker
# for that refactor because every one of them has to be migrated in
# lock-step with the extraction.
#
# Issue 11-A: freeze the current set so no NEW test can import from
# `pearlalgo.trading_bots`. As Issue 1-A lands seams, each migrated test
# drops off this allowlist; the allowlist only shrinks.
#
# To check locally:
#   python scripts/testing/check_test_imports.py
# To update (only when removing a file after migration):
#   remove the path from this list and commit.

# 2026-06-12 grandfather repair: test_confidence_clamp_7a.py,
# test_confidence_literals_5a.py, and test_config_cascade_3a.py have imported
# trading_bots since PRs #38-#40 -- BEFORE the Issue 11-A freeze (#47) -- and
# were missed when this list was created (the guard meta-test has been red on
# main since). Adding them restores the freeze's intent: the list captures the
# legacy set as of the freeze. The only-shrinks ratchet still applies from here.
allowed_tests:
  - tests/test_base_cache.py
  - tests/test_cadence.py
  - tests/test_confidence_clamp_7a.py
  - tests/test_confidence_literals_5a.py
  - tests/test_config_cascade_3a.py
  - tests/test_config_startup_integration.py
  - tests/test_edge_cases.py
  - tests/test_error_recovery.py
  - tests/test_integration.py
  - tests/test_mtf_cache.py
  - tests/test_new_bar_gating.py
  - tests/test_pearl_bot_auto.py
  - tests/test_pipeline_integration.py
  - tests/test_quiet_reason.py
  - tests/test_service_pause.py
  - tests/test_service_smoke.py
  - tests/test_signal_generator_confidence.py
  - tests/test_signal_generator_fallbacks.py
  - tests/test_smc_signals.py
  - tests/test_strategy_indicators.py
  - tests/test_strategy_scenarios.py
  - tests/test_virtual_pnl_tiebreak.py
